web: bin/rails server -p $APP_PORT
jobs: bin/jobs
tunnel: sh -c 'if [ -n "$NGROK_URL" ] && command -v ngrok >/dev/null 2>&1; then exec ngrok http --url=$NGROK_URL $APP_PORT --log stdout; else echo "tunnel disabled (set NGROK_URL and install ngrok to enable)"; exec sleep 86400; fi'
