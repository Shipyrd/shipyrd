en:
  helpers:
    submit:
      update: "Update"
      application:
        create: "Add application"

  activerecord:
    attributes:
      application:
        repository_url: "Repository URL"
      destination:
        url: "URL"
