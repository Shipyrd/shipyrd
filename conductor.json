{
  "scripts": {
    "setup": "bin/conductor-setup",
    "teardown": "bin/conductor-teardown",
    "run": "echo \"Running on port $CONDUCTOR_PORT\" && lsof -ti :$CONDUCTOR_PORT | xargs kill && rm -f tmp/pids/server.pid && PORT=$CONDUCTOR_PORT bin/dev"
  }
}
